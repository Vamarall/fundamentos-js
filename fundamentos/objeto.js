const prod = {} // Objeto vazio

prod.nome = 'Celular Ultra Mega'; // Adiciona a propriedade nome
prod.preco = 4998.90; // Adiciona a propriedade preco

console.log(prod); // Exibe o objeto completo

const prod2 = {
    nome: 'Camisa Polo',
    preco: 79.90,
    desconto: 0.40 // Adiciona a propriedade desconto
};

console.log(prod2); // Exibe o segundo objeto completo
